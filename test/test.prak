/*
    This is a multi-line comment that should not end up in the AST.
*/

"\"Hello world.\"";

var fib = {:n:
    if (n == 0) {
        return 0;
    } else if (n == 1) {
        return 1;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
};

fib 5;
